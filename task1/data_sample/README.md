For tweet text file one tweet per line.
For label file one label per line.

Same format for train, development, test set.


The output of prediction format is 
TweetID,Prediction_Result
One prediction per line
